name = "monochromacy"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

[vars]
# Add your env vars here for local wrangler dev
# ANTHROPIC_API_KEY = "sk-ant-..."
# For production, set via Cloudflare Pages dashboard → Settings → Environment variables
